# Outputs

Runtime outputs are not tracked in Git.

This directory is used for:

- Gradio logs
- training logs
- metrics
- generated comparison images
- temporary visualizations

Keep generated results outside Git unless they are intentionally selected as documentation assets under `docs/assets/`.
